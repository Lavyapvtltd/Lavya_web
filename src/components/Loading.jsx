import React from 'react'
import loading from '/gif/line_loading.gif'

const Loading = ()=> {
        return (
            <div>
                <img src={loading} alt="loading" />
            </div>
        )
}

export default Loading;
